@model Cinema.Models.Menu
@using Cinema.Models

@{
    var categorias = Enum.GetValues(typeof(Categoria)).Cast<Categoria>().ToList();
}

<div class="container mt-3">
    <h3 class="mb-4">Editar Artículo del Menú</h3>
    <form asp-action="Editar" method="post" enctype="multipart/form-data" class="needs-validation" novalidate>
        <input type="hidden" asp-for="Id" />

        <!-- Campo para el nombre del artículo -->
        <div class="mb-3">
            <label asp-for="Articulo" class="form-label">Nombre del Artículo</label>
            <input asp-for="Articulo" class="form-control" required />
            <div class="invalid-feedback">
                Por favor, ingrese el nombre del artículo.
            </div>
        </div>

        <!-- Campo para la descripción -->
        <div class="mb-3">
            <label asp-for="Descripcion" class="form-label">Descripción</label>
            <textarea asp-for="Descripcion" class="form-control" rows="5" required></textarea>
            <div class="invalid-feedback">
                Por favor, ingrese la descripción del artículo.
            </div>
        </div>

        <!-- Campo para el precio -->
        <div class="mb-3">
            <label asp-for="Precio" class="form-label">Precio</label>
            <input asp-for="Precio" class="form-control" required type="number" step="0.01" />
            <div class="invalid-feedback">
                Por favor, ingrese el precio del artículo.
            </div>
        </div>

        <!-- Campo para la categoría -->
        <!-- Campo para la categoría -->
        <div class="mb-3">
            <label asp-for="Categoria" class="form-label">Categoría</label>
            <select asp-for="Categoria" class="form-select" required>
                <option value="">Seleccione una categoría</option>
                @foreach (var categoria in categorias)
                {
                    <option value="@categoria.ToString()">@categoria</option>
                }
            </select>
            <div class="invalid-feedback">
                Por favor, seleccione una categoría.
            </div>
        </div>

        <!-- Campo para la foto -->
        <div class="mb-3">
            <label class="form-label">Foto Actual</label>
            @if (Model.Foto != null && Model.Foto.Length > 0)
            {
                <img src="data:image/jpeg;base64,@Convert.ToBase64String(Model.Foto)" alt="Foto" class="img-thumbnail" style="max-width: 150px;" />
            }
            else
            {
                <div class="d-flex justify-content-center align-items-center h-100 bg-light" style="max-width: 150px;">
                    <p class="text-muted">No hay foto disponible.</p>
                </div>
            }
            <input type="file" name="fotoFile" class="form-control" />
            <div class="invalid-feedback">
                Por favor, suba una foto del artículo si desea actualizarla.
            </div>
        </div>

        <!-- Botón para guardar los cambios -->
        <button type="submit" class="btn btn-primary">
            <i class="bi bi-save"></i> Guardar
        </button>
    </form>
</div>

@section Scripts {
    <script>
        (function () {
            'use strict'
            var forms = document.querySelectorAll('.needs-validation')
            Array.prototype.slice.call(forms)
                .forEach(function (form) {
                    form.addEventListener('submit', function (event) {
                        if (!form.checkValidity()) {
                            event.preventDefault()
                            event.stopPropagation()
                        }
                        form.classList.add('was-validated')
                    }, false)
                })
        })()
    </script>
}
